# Database (Connecting to Docker container from localhost)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=saas_db
DB_USER=postgres
DB_PASSWORD=postgres

# Authentication
JWT_SECRET=dev_secret_key_123
JWT_EXPIRES_IN=24h

# Server
PORT=5000

# Frontend Integration
FRONTEND_URL=http://localhost:3000